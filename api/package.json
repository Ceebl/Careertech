{
  "name": "careertech-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Shared backend process. One router per project, mounted at /api/{project}.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
