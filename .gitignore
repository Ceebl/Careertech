# Local test data. Never commit -- top-level folders get published to the web root.
data/
node_modules/
# Created by the KB upload code when run locally. Committing it would
# publish private screenshots to the public web root.
kb-uploads/

# Detailed infrastructure notes. Kept out of the public repo on purpose.
DOCS.local.md

# TargetConnect capability probes, forum drafts and KB entry drafts.
# Work notes -- not for the public web root.
targetconnect-probe/
