{
  "name": "careertech-tasks",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Private board-and-task app served at /tasks/. Runs in its own container with its own database.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
